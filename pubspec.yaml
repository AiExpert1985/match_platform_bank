name: match_platform_bank
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  excel: ^4.0.6
  flutter_riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0
  file_picker: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner:
  riverpod_generator: 4.0.0+1

flutter:
  uses-material-design: true
